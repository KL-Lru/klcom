# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/repositories/schema.rs"

# オプションがまだ効かないので, コマンド実行時に指定する
# [migrations_directory]
# dir = "db/migrations"